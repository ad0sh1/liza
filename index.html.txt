<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>С днём рождения, солнце... скоро ❤️</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(145deg, #1e1a2f 0%, #2a1e3c 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
            padding: 20px;
        }

        .card {
            max-width: 700px;
            width: 100%;
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border-radius: 50px;
            padding: 40px 30px;
            box-shadow: 0 30px 50px rgba(0, 0, 0, 0.5), 0 0 0 2px rgba(255, 140, 200, 0.2) inset;
            border: 1px solid rgba(255, 200, 230, 0.3);
            text-align: center;
        }

        h1 {
            font-size: 2.2rem;
            margin-bottom: 15px;
            background: linear-gradient(135deg, #f9a8d4, #f472b6);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .subtitle {
            font-size: 1.2rem;
            margin-bottom: 30px;
            opacity: 0.8;
            letter-spacing: 1px;
        }

        /* Таймер */
        .timer {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-bottom: 40px;
            flex-wrap: wrap;
        }

        .timer-item {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 30px;
            padding: 20px 15px;
            min-width: 100px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 200, 230, 0.3);
        }

        .timer-number {
            font-size: 3rem;
            font-weight: bold;
            color: #fda4af;
            line-height: 1;
        }

        .timer-label {
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            opacity: 0.7;
        }

        /* Текст признания */
        .confession {
            background: rgba(0, 0, 0, 0.3);
            border-radius: 40px;
            padding: 30px 25px;
            margin-bottom: 40px;
            border-left: 5px solid #f472b6;
            text-align: left;
            font-size: 1.1rem;
            line-height: 1.7;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }

        .confession p {
            margin-bottom: 15px;
        }

        .confession p:last-child {
            margin-bottom: 0;
            font-style: italic;
            text-align: right;
            color: #f9a8d4;
        }

        /* Кнопки */
        .buttons {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
            justify-content: center;
            margin-bottom: 30px;
        }

        .btn {
            background: linear-gradient(145deg, #ec4899, #d946ef);
            border: none;
            color: white;
            padding: 15px 30px;
            font-size: 1.1rem;
            border-radius: 60px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 8px 20px rgba(236, 72, 153, 0.4);
            font-weight: 600;
            letter-spacing: 0.5px;
            flex: 1 1 auto;
            min-width: 200px;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(236, 72, 153, 0.6);
            background: linear-gradient(145deg, #f472b6, #c026d3);
        }

        .btn:active {
            transform: translateY(0);
        }

        /* Скрытые сообщения (появляются при нажатии) */
        .surprise-message {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 40px;
            padding: 25px;
            margin-top: 25px;
            border: 2px dashed #f9a8d4;
            font-size: 1.3rem;
            line-height: 1.6;
            display: none;
            animation: fadeIn 1s;
            color: #ffe4f3;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .heart {
            color: #f43f5e;
            font-size: 1.5rem;
            display: inline-block;
            margin: 0 5px;
        }

        footer {
            margin-top: 20px;
            font-size: 0.9rem;
            opacity: 0.5;
        }
    </style>
</head>
<body>
    <div class="card">
        <h1>С днём рождения, Лизочка! ❤️</h1>
        <div class="subtitle">Твой особенный день уже близко...</div>

        <!-- Таймер обратного отсчета до 28 февраля -->
        <div class="timer" id="timer">
            <div class="timer-item">
                <div class="timer-number" id="days">00</div>
                <div class="timer-label">дней</div>
            </div>
            <div class="timer-item">
                <div class="timer-number" id="hours">00</div>
                <div class="timer-label">часов</div>
            </div>
            <div class="timer-item">
                <div class="timer-number" id="minutes">00</div>
                <div class="timer-label">минут</div>
            </div>
            <div class="timer-item">
                <div class="timer-number" id="seconds">00</div>
                <div class="timer-label">секунд</div>
            </div>
        </div>

        <!-- Твое признание в чувствах -->
        <div class="confession">
            <p>Лиза, в этот день, когда мир стал чуточку лучше, потому что родилась ты, я хочу сказать тебе то, что давно лежит на сердце.</p>
            <p>Наше знакомство — самое прекрасное, что случилось со мной в последнее время. Я чувствую эту невероятную связь между нами и вижу огоньки в твоих глазах, когда мы рядом. Эти чувства взаимны, я это знаю.</p>
            <p>Я не хочу больше ждать идеального момента. Этот момент — сейчас, в твой день. Я готов к этому шагу. Я готов быть с тобой.</p>
            <p>С днём рождения, моя хорошая. Ты невероятная. ✨</p>
            <p>— Коля</p>
        </div>

        <!-- Интерактивные кнопки -->
        <div class="buttons">
            <button class="btn" id="btnYes">💖 Нажми, если тоже хочешь</button>
            <button class="btn" id="btnDate">💫 Хочу увидеть нашу дату</button>
        </div>

        <!-- Скрытые сообщения, которые появятся при клике -->
        <div id="surpriseYes" class="surprise-message">
            <span class="heart">❤️</span> Я так счастлив! Тогда давай сделаем это официально? Ты согласна начать наши отношения с этого дня? <span class="heart">❤️</span>
        </div>

        <div id="surpriseDate" class="surprise-message">
            <span class="heart">✨</span> Как насчёт 1 марта? Встретимся и отметим уже не только твой день рождения, но и наш первый день как пара? <span class="heart">✨</span>
        </div>

        <footer>для тебя, с любовью</footer>
    </div>

    <script>
        // 1. НАСТРОЙКА ТАЙМЕРА
        // Устанавливаем целевую дату — 28 февраля этого или следующего года
        function getTargetDate() {
            const today = new Date();
            let targetYear = today.getFullYear();
            // Месяцы в JS идут с 0: 1 = февраль (потому что 0 - январь)
            const targetDate = new Date(targetYear, 1, 28); // 28 февраля текущего года

            // Если сегодня уже позже 28 февраля (например, 1 марта), то переносим на следующий год
            if (today > targetDate) {
                targetDate.setFullYear(targetYear + 1);
            }
            return targetDate;
        }

        function updateTimer() {
            const targetDate = getTargetDate();
            const now = new Date();
            const diff = targetDate - now; // разница в миллисекундах

            if (diff <= 0) {
                // Если вдруг наступил день рождения
                document.getElementById('days').innerText = '00';
                document.getElementById('hours').innerText = '00';
                document.getElementById('minutes').innerText = '00';
                document.getElementById('seconds').innerText = '00';
                return;
            }

            const days = Math.floor(diff / (1000 * 60 * 60 * 24));
            const hours = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((diff % (1000 * 60)) / 1000);

            document.getElementById('days').innerText = days.toString().padStart(2, '0');
            document.getElementById('hours').innerText = hours.toString().padStart(2, '0');
            document.getElementById('minutes').innerText = minutes.toString().padStart(2, '0');
            document.getElementById('seconds').innerText = seconds.toString().padStart(2, '0');
        }

        // Обновляем таймер каждую секунду
        updateTimer();
        setInterval(updateTimer, 1000);

        // 2. ИНТЕРАКТИВНЫЕ КНОПКИ
        document.getElementById('btnYes').addEventListener('click', function() {
            const msg = document.getElementById('surpriseYes');
            msg.style.display = 'block';
            // Плавно прокручиваем к сообщению
            msg.scrollIntoView({ behavior: 'smooth', block: 'center' });
        });

        document.getElementById('btnDate').addEventListener('click', function() {
            const msg = document.getElementById('surpriseDate');
            msg.style.display = 'block';
            msg.scrollIntoView({ behavior: 'smooth', block: 'center' });
        });
    </script>
</body>
</html>